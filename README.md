Структура файла user.js

const email = "";
const password = "";
const incorrectEmail = "incorrectEmail@mail.ru";
const incorrectPassport = "incorrectPassport";

module.exports = { email, password, incorrectEmail, incorrectPassport };
